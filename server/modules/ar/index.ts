/**
 * AR Digital Business Card Module
 * 
 * Features:
 * - AR target compilation from business card images
 * - MindAR integration for WebAR experiences
 * - Cloudinary CDN upload for .mind files and textures
 * - External AR compiler proxy support
 * 
 * Environment Variables:
 * - CLOUDINARY_URL: Cloudinary connection string
 * - AR_COMPILER_PROXY_URL: External AR compilation service URL (optional)
 * - VITE_AR_DEFAULT_POSTER: Default poster URL for AR elements (optional)
 */

import router from './routes';

export default router;