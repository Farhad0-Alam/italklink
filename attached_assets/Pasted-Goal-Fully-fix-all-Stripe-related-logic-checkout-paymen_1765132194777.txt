Goal: Fully fix all Stripe-related logic (checkout payment, pricing, and plans/subscriptions) for iTalkLink, but SKIP the shop_cart table.

Scope:
- You can modify ONLY Stripe-related code: billing routes, Stripe config, plan/pricing logic, and webhooks.
- Do NOT create or touch the `shop_cart` table or any cart system.
- Do NOT refactor UI or unrelated backend modules.

Tasks:

1) Fix payment method & checkout errors:
   - In server/billing-routes.ts (and any related files), remove or correct the invalid `payment_method_options.acss_debit` block that causes
     `invalid_payment_method_options` and `transaction_type` errors.
   - Use only valid Stripe options (for example, card-based checkout) and ensure `transaction_type` is either 'personal' or 'business'.
   - Make sure checkout sessions are created without any Stripe validation errors.

2) Verify & fix pricing / plans:
   - Audit all Stripe price IDs and plan mapping (Free / Pro / Team / etc.).
   - Ensure each plan uses the correct Stripe `price` ID, `mode` ('subscription' for recurring, 'payment' if one-time), and proper `success_url` and `cancel_url`.
   - Fix any hard-coded or wrong price IDs so that upgrading/downgrading plans works as expected.
   - Keep using existing Stripe products/prices; do NOT create new ones in the dashboard. Just fix the code mapping.

3) Subscription & webhooks:
   - Check webhook handlers for `checkout.session.completed`, `customer.subscription.created/updated`, etc.
   - Make sure that after successful payment:
       - The user's plan in the database is updated correctly.
       - Trial / cancel states are handled safely (no crashes if webhook runs twice).
   - Do NOT depend on any `shop_cart` table.

4) Tests:
   - Run Stripe test checkout for each paid plan.
   - Confirm:
       ✔ Checkout session creates successfully
       ✔ No `invalid_payment_method_options` or `transaction_type` errors
       ✔ User plan is updated in the database after payment
   - Print a short summary of what files were changed and what was fixed.

Very important:
- SKIP any work related to `shop_cart` or cart/multi-item features.
- Do NOT refactor or rename unrelated functions, files, or components.
- Only change the minimum code required to make Stripe payment, pricing, and plan logic fully functional.
