You are an expert full-stack engineer helping me plan a Digital Product Shop system inside my existing TalkLink project on Replit.

❗Important Constraints

Frontend: Vite + React + TypeScript

Backend: Express.js

Routing: Wouter

State management: React Query v5

UI: Tailwind CSS + shadcn/ui

Database: Replit PostgreSQL ONLY

File storage: Replit server filesystem only

Digital files must be stored in a local /server/uploads folder

No Supabase, no BunnyCDN, no Cloudflare R2, no external storage at all

External APIs:

Stripe for payments (allowed)

No other external storage/database APIs

🎯 Goal

Create a detailed, structured TASK LIST only (no code yet) for integrating a full Digital Product Shop system into the existing TalkLink codebase.

The system includes:

Public shop element on a user’s digital card

Secure digital product selling & delivery

Seller dashboard (for product owners)

Admin dashboard (platform control)

User dashboard extensions (orders, downloads, billing)

Internal-only storage (Replit DB + filesystem)

Stripe checkout + webhook + download token flows

📌 What you should do

Scan the existing project structure and codebase to understand:

How auth works (user model, roles, sessions/JWT)

How routing is handled in React (Wouter routes)

How the current User Dashboard and Admin Dashboard are structured

Existing database helpers (for Replit PostgreSQL)

Existing UI components (buttons, cards, layout, etc.)

Based on that, produce a clear, ordered, implementation-ready task list to add the Digital Product Shop system.

Do NOT write any code yet.
Only output a structured task plan.

🧱 Functional Scope (What the system must support)

Please cover all of these in the task list:

A. Database Design (Replit PostgreSQL)

Create tasks to design and migrate DB tables for:

products

Digital products owned by a seller (card owner)

Fields like: id, user_id, title, slug, descriptions, price, discount_price, currency, category, thumbnail_url, file_path (local /uploads/...), file_size, status, views, purchases, timestamps

orders

Stripe checkout sessions and purchases

Fields like: id, product_id, seller_id, buyer_id and/or buyer_email, amount, currency, stripe_session_id, payment_status, download_token, timestamps

downloads

Control secure download access

Fields like: id, user_id, product_id, order_id, file_path, download_token, download_count, max_downloads, expires_at, timestamps

billing

Stripe billing history per user

Fields like: id, user_id, amount, currency, payment_intent/session, timestamps

analytics (optional but recommended)

Events like view, click, purchase, download

Fields like: id, user_id, product_id, event, meta JSON, timestamps

(Optional) payouts for seller revenue/commission accounting (for future)

B. Roles & Auth

Create tasks to:

Ensure the users table supports roles:

user, seller, admin

Add / confirm backend middlewares:

authRequired (for logged-in users)

isSeller (for seller dashboard)

isAdmin (for admin dashboard)

Wire req.user shape (id, email, role) from the existing auth system into all new routes.

C. File Storage (Replit Filesystem ONLY)

Create tasks to:

Configure a local uploads folder:

/server/uploads

Add an Express file upload route using multer:

POST /api/upload

Saves incoming file to /server/uploads

Returns file_path, file_size, original_name

Add Express static/file serving or res.download logic:

For serving files at /api/download/:token

Uses file_path on local filesystem

Define cleanup/retention strategy (optional) for old files (as tasks, not code).

D. Backend API – Core Shop Logic

Create tasks for adding Express routes for:

1. Product Management (Seller & Internal)

POST /api/products/create – Create a product (seller only)

POST /api/products/update/:id – Update product

POST /api/products/delete/:id – Soft delete or hard delete

GET /api/products/:userId – Public list of active products for a given card owner (for Shop Element on TalkLink card)

GET /api/products/single/:id – Single product details (for editing/preview)

2. Checkout & Stripe Webhook

POST /api/checkout

Takes product_id and buyer_email

Looks up product in DB

Creates Stripe Checkout Session

Redirect URL: TalkLink frontend success URL (e.g. /download?session_id=...)

POST /api/webhook/stripe

Validates Stripe webhook signature

On checkout.session.completed:

Finds the product and seller

Creates an orders row

Creates a downloads row with:

download_token

file_path (local file)

max_downloads and expires_at

Increments product purchase count

Logs analytics event

Inserts a billing row

(Optional) Triggers an email with the download link

3. Secure Download Route

GET /api/download/:token

Validates download_token

Checks expires_at and max_downloads

Increments download_count

Uses file_path from DB to call res.download() from /server/uploads

E. Backend API – Dashboards

Create tasks for:

1. Seller Dashboard Routes

GET /api/seller/products – all products for req.user.id

GET /api/seller/orders – all orders where seller_id = req.user.id

GET /api/seller/analytics – revenue, sales, views, conversion for seller

2. Admin Dashboard Routes

GET /api/admin/users – all users

POST /api/admin/users/disable / enable

GET /api/admin/products – all products with seller info

POST /api/admin/products/disable / enable / delete

GET /api/admin/orders – all orders with seller & product info

GET /api/admin/analytics – platform-wide metrics

3. User Dashboard Routes (normal users)

GET /api/user/orders – orders where this user is the buyer

GET /api/user/purchases – purchased products with download tokens

GET /api/user/downloads – download history and limits

GET /api/user/billing – billing entries tied to this user

F. Frontend – Shop Element on TalkLink Card

Create tasks to add React components and hooks:

Hooks

useProducts(userId) using React Query for /api/products/:userId

useCheckout() to call /api/checkout

Components

<ShopElement />

Props: ownerUserId, sectionTitle, sectionDescription, layout, maxItems, defaultBuyerEmail

Shows product cards in responsive grid/list

Search/filter support

<ProductCard />

Thumbnail, title, short description, price, sold count

<ProductModal />

Expanded view with description, price, email field, “Buy & Instant Access” button

Calls useCheckout and redirects to Stripe Checkout

Insert <ShopElement /> into the existing TalkLink card/page layout for each card owner.

G. Frontend – Seller Dashboard UI

Create tasks to:

Add Seller Dashboard pages:

/dashboard/seller/products

/dashboard/seller/products/create

/dashboard/seller/products/edit/:id

/dashboard/seller/orders

/dashboard/seller/analytics

Components & features:

Product listing table (title, price, status, sales)

Create/Edit product form

Includes file upload UI → calls /api/upload

Stores file_path into product

Orders table

Analytics cards (total revenue, total sales, total views, conversion rate)

Add navigation/menu entry for seller users only based on user.role === "seller".

H. Frontend – Admin Dashboard UI (Shop-related)

Create tasks to:

Add Admin pages:

/admin/dashboard

/admin/users

/admin/products

/admin/orders

/admin/analytics

Add UI:

Tables for users, products, orders

Action buttons: Disable/Enable/Delete (for users & products)

Analytics overview cards (total revenue, total orders, total users, active products)

Top products list

Show these pages only to user.role === "admin" users.

I. Frontend – User Dashboard Extensions

Create tasks to extend existing User Dashboard with Shop-related sections for all logged-in users:

Add new menu items:

“My Orders”

“My Purchases”

“My Downloads”

“Billing”

Add pages & UI:

/user/orders → uses /api/user/orders

/user/purchases → list of purchased products + Download button

/user/downloads → download token list with remaining downloads

/user/billing → billing transactions from /api/user/billing

J. Configuration & ENV

Create tasks to:

Add and validate env variables:

DATABASE_URL

STRIPE_SECRET_KEY

STRIPE_WEBHOOK_SECRET

FRONTEND_URL

VITE_API_URL

Ensure Vite frontend uses VITE_API_URL to talk to the Express backend.

K. Security, Validation & QA

Create tasks to:

Protect all routes with appropriate middlewares:

authRequired for user routes

isSeller for seller routes

isAdmin for admin routes

Add basic input validation for:

Product creation

Checkout request

File upload (size, type checks – as tasks)

Add logging for:

Stripe webhook events

Download failures

File missing cases

Plan a basic manual test suite:

Create product, buy it, receive email link (if email is implemented), download file

Check download limit & expiry behavior

Ensure seller and admin dashboards show correct stats