Goal: Fix the Stripe checkout so it works using ONLY normal card payments. 
Remove all usage of acss_debit, au_becs_debit, or any bank debit payment_method_options that are not enabled.
Do NOT touch or create the `shop_cart` table. Do NOT change unrelated UI or business logic.

Context:
- The current checkout returns 500 with an error about `au_becs_debit` payment method not being activated.
- Previously, `transaction_type: 'one_time'` was changed to `'personal'`, but the error persists.

Tasks:

1) Find and clean ALL non-card payment method usage:
   - Search the whole project for:
       - `acss_debit`
       - `au_becs_debit`
       - `payment_method_options`
   - In all Stripe-related files (for example: `server/billing-routes.ts`, `server/stripe.ts`, or helpers), remove or disable any 
     `payment_method_options` blocks that configure `acss_debit` or `au_becs_debit`.
   - Ensure the checkout/session or payment intent uses:
       - `payment_method_types: ['card']` only (or the default card-only behavior).
       - No acss/au_becs or bank debit options at all.

2) Normalize checkout session creation:
   - For each plan/price (Free/Pro/Custom, etc.) make sure:
       - `mode` is `'subscription'` for recurring plans.
       - The correct Stripe `price` IDs from ENV/config are used.
       - `success_url` and `cancel_url` are valid.
       - Any metadata like `transaction_type` is set to allowed values (`'personal'` or `'business'`) or removed if not needed.
   - Do NOT create new products or prices in Stripe; just fix the mapping in code.

3) Improve error handling (avoid raw 500 on Stripe errors):
   - Wrap checkout/session creation in try/catch.
   - On Stripe error:
       - Log the full Stripe error on the server (console).
       - Return a 400 status with a clean JSON `{ message: 'Stripe checkout failed', detail: stripeError.message }`
         instead of an unhandled 500.
   - Do NOT change any non-Stripe routes.

4) Tests:
   - Run the app in test mode.
   - From the pricing page, test subscribing to each paid plan.
   - Confirm:
       ✔ No error about `acss_debit` or `au_becs_debit`
       ✔ No `invalid_payment_method` or `invalid_request_error`
       ✔ Stripe Checkout page opens normally with card payment
   - Print a short summary of:
       - Files changed
       - Code blocks removed/updated
       - Final payment_method_types used

Very important:
- Do NOT create or use the `shop_cart` table.
- Do NOT refactor or rename unrelated functions, components, or routes.
- Only make the minimum necessary changes to Stripe-related code to get card-based checkout working reliably.
