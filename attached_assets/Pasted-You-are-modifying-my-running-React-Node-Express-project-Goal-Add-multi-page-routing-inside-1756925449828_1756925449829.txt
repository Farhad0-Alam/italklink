You are modifying my running React + Node (Express) project.

Goal:
Add multi-page routing inside a single URL (/card/:slug) AND a reusable, styleable "Menu Element" that supports both internal pages and external links. Menu design must be editable via my "Add Element" builder (style tokens + presets).

Scope:

1) Routing (React Router v6):
- Create nested routes:
  /card/:slug            -> Overview page (index)
  /card/:slug/about      -> About
  /card/:slug/services   -> Services
  /card/:slug/gallery    -> Gallery
  /card/:slug/contact    -> Contact
- Create a layout component `CardLayout` that fetches the card by slug and renders the Menu Element + <Outlet /> for sub-pages.
- If a route key is not present in config, do not render it in the menu.

2) Backend API (Node/Express):
- Extend existing GET /api/cards/:slug to return:
  {
    ok: true,
    card: {
      id, slug, title, brand,
      pages: [ { key, path, label, visible } ],
      menu: [
        {
          id, type: "internal"|"external",
          label,
          path,       // for internal (e.g. "", "about", "services")
          href,       // for external (e.g. "https://…")
          target,     // "_self" | "_blank"
          rel,        // "noopener noreferrer nofollow" (optional)
          icon,       // optional icon name
          order: number,
          visible: boolean,
          style: {
            variant: "tabs" | "pills" | "underline" | "ghost",
            orientation: "horizontal" | "vertical",
            radius: "none"|"sm"|"md"|"lg"|"xl"|"2xl",
            size: "sm"|"md"|"lg",
            gap: number,
            weightActive: number,   // font-weight active
            weight: number,         // font-weight default
            underlineActive: boolean,
            shadow: boolean,
            sticky: boolean,
            mobileCollapse: boolean,
            bg: string, fg: string, fgActive: string, border: string
          }
        }
      ]
    }
  }

3) Menu Element (frontend):
- Create a reusable component `MenuElement` at `client/src/elements/MenuElement.tsx`.
- Props:
  {
    baseUrl: string; // e.g. `/card/${slug}`
    items: MenuItem[]; // shape described above
    onTrack?: (eventName:string, payload:any)=>void; // for analytics
  }
- Behavior:
  - Render items by `order`, only if `visible === true`.
  - If item.type === "internal": link to `${baseUrl}` (for path="") or `${baseUrl}/${path}`.
  - If item.type === "external": render <a href={href} target={target ?? "_blank"} rel={rel ?? "noopener noreferrer"}>.
  - Highlight active item by current location (for internal routes).
  - Support keyboard navigation (a11y) and ARIA roles for nav.
  - Respect item.style + global style tokens. If Tailwind is present, use Tailwind utility classes; otherwise use inline styles/CSS modules.
  - Mobile: if `style.mobileCollapse` true, render a hamburger dropdown.
  - If `style.sticky` true, make the menu sticky to top.

- Provide 4 presets inside the component:
  "tabs", "pills", "underline", "ghost" (use sensible defaults).

4) Card Layout:
- Create `client/src/modules/cards/components/CardLayout.tsx`:
  - Fetch `/api/cards/:slug`.
  - Render brand header + <MenuElement baseUrl={`/card/${slug}`} items={card.menu} />.
  - `<Outlet />` for nested routes (Overview/About/Services/Gallery/Contact).
- Create five stub pages under `client/src/modules/cards/pages/` for Overview, About, Services, Gallery, Contact.

5) Add Element builder integration:
- Register this as a new element type "menu" in the builder registry:
  `client/src/builder/registerElements.ts` (or the existing registry file):
    addElement("menu", {
      component: MenuElement,
      inspector: [
        // Expose editable controls for:
        // variant, orientation, radius, size, gap, sticky, mobileCollapse, bg, fg, fgActive, border
        // Also allow CRUD on items: label, type, path/href, target, icon, visible, order
        // Provide live preview on change
      ],
      defaults: {
        variant: "tabs",
        orientation: "horizontal",
        radius: "lg",
        size: "md",
        gap: 8,
        underlineActive: true,
        shadow: false,
        sticky: true,
        mobileCollapse: true,
        bg: "transparent",
        fg: "#0f172a",
        fgActive: "#111827",
        border: "rgba(0,0,0,0.08)"
      }
    });
- Persist/Load the `card.menu` array via existing card update APIs (PUT /api/cards/:id), without breaking other fields.

6) Router wiring:
- Add `CardRoutes`:
  client/src/modules/cards/CardRoutes.tsx with:
    <Route path="/card/:slug" element={<CardLayout/>}>
      <Route index element={<Overview/>} />
      <Route path="about" element={<About/>} />
      <Route path="services" element={<Services/>} />
      <Route path="gallery" element={<Gallery/>} />
      <Route path="contact" element={<Contact/>} />
    </Route>
- Mount CardRoutes into the app router.

7) Styling notes:
- If Tailwind is installed, prefer utilities (rounded-2xl, px-3, py-2, gap-x-3, underline decoration-2, shadow etc).
- For active states:
  - tabs: bold + bottom border
  - pills: rounded + filled bgActive
  - underline: text underline for active
  - ghost: subtle weight change + opacity
- Respect brand color: fall back to card.brand.color for active accents.

8) Analytics:
- Expose onTrack hook on MenuElement and call it on each click:
  onTrack("menu_click", { slug, itemId, type, path, href })

9) Acceptance criteria:
- Visiting /card/:slug shows a styled menu; clicking internal items routes without full reload.
- External link items open in new tab with rel="noopener noreferrer" and do not affect router.
- Menu appearance can be fully controlled from the Add Element builder (variant, colors, radius, gap, sticky, collapse, etc.).
- No breaking changes to existing card APIs or pages.
- Code added in modular folders:
  client/src/elements/MenuElement.tsx
  client/src/modules/cards/components/CardLayout.tsx
  client/src/modules/cards/pages/{Overview,About,Services,Gallery,Contact}.tsx
  client/src/modules/cards/CardRoutes.tsx
- Update only what is necessary; keep existing features intact.

Implement now and show me:
- New/updated files (paths)
- Key code snippets
- How to add/edit menu items from the builder UI
