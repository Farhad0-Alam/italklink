You are a senior DevOps + full-stack fixer inside my Replit project. The app crashed after I asked you to add a “collapse/accordion toggle on subsection.” I need an exact rollback to the last known good state before that request. Do a safe, auditable revert with backups.

Objectives

Identify the last successful commit/run before the change that mentions collapse/accordion toggle on subsection.

Backup current broken state to a branch, and stash any uncommitted work.

Hard reset to the stable commit.

Clean install & rebuild, revert accidental schema/migration changes if any, and verify the app runs.

Report exactly what you changed and how to re-apply the feature later safely.

Constraints / Do NOT

❌ Do NOT modify .env* or any secrets (Stripe, Supabase, etc.).

❌ Do NOT delete data; if migrations were added by the broken change, roll them down only.

❌ Do NOT remove logs/history.

Step-by-Step (execute automatically)

Detect target commit

Search git history for commits after the last green build and locate the one(s) referencing: collapse, accordion, toggle, subsection.

Pick the last known good commit just before those changes. Call it GOOD_COMMIT.

Safety backups

Create a backup branch from current HEAD:
git checkout -b wip-broken-collapse-rollback-$(date +%Y%m%d-%H%M)

Stash any workspace/uncommitted changes:
git add -A && git stash push -m "stash before rollback (collapse toggle break)"

Hard rollback

git checkout main (or default branch)

git reset --hard GOOD_COMMIT

Remove lockfiles and modules to avoid drift:
rm -rf node_modules package-lock.json pnpm-lock.yaml yarn.lock

Install deps fresh:
Use the project’s package manager (detect): npm ci || pnpm install || yarn install

DB & schema sanity (if the broken change added migrations)

Detect pending or newly added migrations after GOOD_COMMIT.

If any were introduced by the broken change, run down/rollback to match GOOD_COMMIT schema.
(Drizzle/Prisma: run the appropriate down/restore command.)

Rebuild & verify

Build: npm run build

Lint & typecheck: npm run lint || true, npm run typecheck || true

Run tests (if present): npm test || true

Start the app and confirm health on the usual port. Capture logs.

Clean artifacts

Clear any framework/.next/.vite caches as needed (detect framework):
rm -rf .next dist build .turbo && npm run build (only if applicable)

Acceptance checks (must pass)

App boots without crash.

No imports/components named Accordion, Collapse, Disclosure, or similar are present in the current diff.

Routes, dashboard, and KB ingestion screens load and function.

No schema drift versus GOOD_COMMIT (report diff if any).

Output a concise report

Print:

GOOD_COMMIT hash + message + date.

Commands executed.

Any migrations rolled back.

Next steps to safely re-implement the collapse feature (create a feature branch, add unit test, and UI guard).

(Optional) Tag stable point

Create a tag stable-before-collapse-$(date +%Y%m%d) at GOOD_COMMIT.

If something blocks you

If GOOD_COMMIT cannot be auto-detected, list the last 15 commits with dates & messages and pause for my selection.

Post-rollback guidance (include in the report)

Create a new branch feat/subsection-collapse from the stable main.

Add the UI change behind a feature flag in config.

Add a component-level test to ensure the toggle cannot break render.

Re-run build & tests before opening a PR.

Now perform the rollback and provide the report.