You are an expert React + TypeScript + Tailwind + shadcn/ui developer.

Project goal: Fix and upgrade the contact form builder field “Type” functionality in:
- client/src/components/page-element.tsx
specifically inside the ContactFormRenderer component.

Right now, the visual editor shows these Type options:
- Text
- Email
- Phone
- Textarea
- Date
- Dropdown
- Checkbox

But the implementation still only fully supports "text" | "email" | "tel" | "textarea".  
Date, Dropdown, and Checkbox are not working correctly in view mode and not fully saved in the config.

====================
TASKS (VERY IMPORTANT)
====================

1) **Extend FieldType and FieldConfig**
--------------------------------------
Inside ContactFormRenderer, update the FieldType and FieldConfig so they support all types:

- Update:
  type FieldType = "text" | "email" | "tel" | "textarea";
to:
  type FieldType = "text" | "email" | "tel" | "textarea" | "date" | "dropdown" | "checkbox";

- Extend FieldConfig with optional props for advanced types:
  - `options?: string[];`  // for dropdown values
  - `checkedLabel?: string;` // optional label for checkbox checked state (future use)
  - `uncheckedLabel?: string;` // optional label for checkbox unchecked state (future use)

Make sure the existing keys (name, email, phone, subject, message) still work and stay backward compatible.

2) **Normalize fieldConfigs with new types**
-------------------------------------------
In `normalizeFieldConfigs()`:

- Keep the same backward compatibility logic, but also allow `f.type` to be "date", "dropdown", or "checkbox".
- When you merge `saved` configs, validate `f.type` against the new FieldType union instead of just ["text", "email", "tel", "textarea"].
- If a saved field has `type: "dropdown"` but no `options`, default `options` to something like:
  ["Option 1", "Option 2"] so the dropdown is usable by default.

3) **Update EDIT MODE UI (visual editor)**
-----------------------------------------
In the isEditing block where each field's settings are rendered:

- In the `<Select>` for field type, ensure the following SelectItems exist and are wired to the FieldType:
  - value="text" label "Text"
  - value="email" label "Email"
  - value="tel" label "Phone"
  - value="textarea" label "Textarea"
  - value="date" label "Date"
  - value="dropdown" label "Dropdown"
  - value="checkbox" label "Checkbox"

- For dropdown fields:
  - Under the existing label/placeholder/type controls, add a small config UI that only appears when `f.type === "dropdown"`.
  - Use an Input to edit a comma-separated string of options, e.g. "Option 1, Option 2, Option 3".
  - When this input changes, split on commas, trim whitespace, and update `options` on the field:
    `setField(f.key, { options: value.split(",").map((o) => o.trim()).filter(Boolean) })`.

- For checkbox fields:
  - You DON’T need extra text fields right now, but you must show a hint text like:
    “This will render as a single checkbox (true/false) in the form.”
  - The `required` toggle should still work: if required, the checkbox must be checked to submit.

- After updating a field (label, placeholder, type, required, options, etc.), keep the existing behavior:
  - Update both `fieldConfigs` in element.data
  - Update the legacy `fields` array (only enabled fields) to keep backward compatibility.

4) **Update VIEW MODE (actual form render)**
-------------------------------------------
In the non-edit (view) block, where each enabled field is rendered:

Currently you have special handling for textarea vs other input types.  
Extend it for new field types:

- Compute a shared `commonStyle` object for border/background/text color (it already exists).
- For each field `f` in `enabledFields`:

  a) If `f.type === "textarea"` -> keep existing <textarea> logic.

  b) If `f.type === "dropdown"`:
     - Render a `<select>` element instead of `<input>`.
     - Use `f.options || []` as the options list.
     - Include a default `<option value="">Select...</option>` when not required or no value yet.
     - When the user changes selection, update `formData[f.key]` with `e.target.value`.

  c) If `f.type === "checkbox"`:
     - Render a single checkbox input:
       `<input type="checkbox" ... />`
     - Checked state comes from `!!formData[f.key]`.
     - On change, set `formData[f.key]` to `e.target.checked`.
     - For required checkboxes, enforce before submit that it must be true
       (if `f.required` is true and `!formData[f.key]`, show validation error / block submit).

  d) If `f.type === "date"`:
     - Render `<input type="date" ... />`.
     - Value is `formData[f.key] || ""`.
     - On change, store the selected date string as is.

  e) For all other input types (`text`, `email`, `tel`), keep the existing <input> rendering logic.

5) **Update validation in handleFormSubmit**
-------------------------------------------
In the submit handler (`handleFormSubmit`):

- When you loop over enabled fields and check required fields:
  - For most types, required means `formData[f.key]` is a non-empty string.
  - For checkbox type, required means `formData[f.key] === true`.
  - For dropdown, required means `formData[f.key]` is a non-empty string (not just "").

Implement this logic so required validation works correctly with the new field types.

6) **Do NOT break existing forms**
----------------------------------
- Old saved data may not have `type` set to "date", "dropdown", or "checkbox".
- If a field has an unrecognized type, fallback safely to "text".
- Ensure that `normalizeFieldConfigs()` always returns configs for name, email, phone, subject, message with safe defaults so existing pages still render.

7) **Fix double-curly placeholder string bug (small extra fix)**
----------------------------------------------------------------
There is a runtime error "from_name is not defined" coming from a JSX label text like:

  Message (supports {{name}}, {{from_name}})

React is trying to interpret `{{` as an object. Fix it by making it a plain string, for example:

  <label className="...">
    {"Message (supports {{name}}, {{from_name}})"}
  </label>

Or escape it in whatever way keeps the literal `{{name}}` and `{{from_name}}` visible in the UI but does NOT try to read any variables.

8) **Run TypeScript & build**
-----------------------------
- Make sure `page-element.tsx` compiles with TypeScript without any type errors.
- Run the dev server and ensure:
  - The Type dropdown in the visual editor can switch between all 7 options.
  - Dropdown fields show options and save them.
  - Checkbox fields work and can be required.
  - Date fields render correctly.
  - Form submit still posts to `/api/contact-form/submit` (or existing endpoint) with all fields in `formData`.
  - No more runtime error about `from_name`.

Please implement all of these changes directly in client/src/components/page-element.tsx, reusing the existing styling and components (Input, Select, SelectTrigger, SelectContent, SelectItem, Switch, etc.) so the UI stays consistent with the rest of the builder.
